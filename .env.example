# Backend API base URL — set this in Vercel's Environment Variables dashboard
# Example: https://your-api.onrender.com
VITE_API_BASE_URL=https://your-backend-url.com

# Google OAuth client ID (optional — Google login button appears only when set)
VITE_GOOGLE_CLIENT_ID=
